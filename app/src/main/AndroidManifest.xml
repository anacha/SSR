<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.kth.ssr" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="se.kth.ssr.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ViewDirectoryActivity"
            android:label="@string/title_activity_select_voice" >
        </activity>

        <activity
            android:name=".activities.PlayRecordingActivityDefault"
            android:label="@string/title_activity_select_voice" >
        </activity>
        <activity
            android:name=".activities.RecordingActivityDefault"
            android:label="@string/title_activity_select_voice" >
        </activity>
        <activity
            android:name=".activities.BreakRecordingToPiecesActivityDefault"
            android:label="@string/title_activity_select_voice" >
        </activity>

    </application>

</manifest>
